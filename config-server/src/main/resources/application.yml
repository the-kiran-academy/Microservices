server:
  port: 9190
  
spring:
  application:
    name: CONFIG-SERVER 
    
  cloud:
    config:
      server:
        git:
          uri: https://github.com/ram-chadar/config-server-file
          clone-on-start: true   